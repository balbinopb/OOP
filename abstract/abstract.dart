
abstract class calculate {
  
  int areaRectangle(int length ,int width ); //this one we call abstract
  double areaCircle(double phi,int radius); //this one we call abstract
  double areaTriangle(int base,int height); //this one we call abstract

}
